"use client";
import React from "react";

interface Props {}

const TodoUserListPage = ({}: Props) => {
  return (
    <main className="text-center">
      <h1 className="text-sm text-red-400">To Do List</h1>
    </main>
  );
};

export default TodoUserListPage;
